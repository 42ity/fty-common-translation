# fty-common-translation

This is a library providing :
* A translation system based on idea of gettext

## How to build

To build the `fty-common-translation` project run:

```bash
cmake -B build -DBUILD_TESTING=On
cd build/
make
ctest # to run self-test
```
